package analyzer;

/**
 * Token 类型枚举。
 */
public enum TokenType {
    KEYWORD,
    IDENTIFIER,
    INTEGER,
    FLOAT,
    STRING,
    CHAR,
    OPERATOR,
    DELIMITER,
    COMMENT,
    EOF,
    ERROR
}
